#include <stdio.h>
int main(){
    int x, y, sum;
    printf("enter the first number: ");
    scanf("%d", &x);
    printf("enter the seconde number:");
    scanf("%d",&y);
    sum= x+y;
    printf("your sum is: %d", sum);
    return 0;
    
}